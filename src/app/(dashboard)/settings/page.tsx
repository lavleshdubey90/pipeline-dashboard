import type React from "react";
import Appearance from "@/app/(dashboard)/settings/Appearance";

const Settings: React.FC = () => {
  return (
    <div>
      <Appearance />
    </div>
  );
};

export default Settings;
